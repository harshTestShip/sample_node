# Language setting
language: node_js


# Version number
node_js:
  - 0.12

env:
  global:
    - FOO=BAR
    - BOO=FAR
  matrix:
    - M1=ONE

build:
  ci:
    - ps -eax
    - ls -al
    - echo "$*FOO" 


